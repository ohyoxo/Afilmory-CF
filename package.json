{
  "name": "afilmory-workers",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "description": "Afilmory photo gallery for Cloudflare Workers",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "npm run build:frontend && npm run build:worker",
    "build:frontend": "cd apps/web && npm install && npm run build:workers",
    "build:worker": "echo 'Worker build completed'",
    "type-check": "tsc --noEmit",
    "lint": "echo 'Linting skipped'",
    "format": "echo 'Formatting skipped'",
    "test": "echo 'Tests skipped'",
    "setup": "tsx scripts/setup-workers.ts",
    "setup:r2": "wrangler r2 bucket create afilmory-photos && wrangler r2 bucket create afilmory-cache",
    "migrate": "tsx scripts/migrate-to-workers.ts"
  },
  "dependencies": {
    "@cloudflare/workers-types": "^4.20241218.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3",
    "tsx": "^4.20.3",
    "wrangler": "^3.78.12"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}